var x = 10;
var y = 15;

var total = x + y;

var nome = "Vitor Lucas";
var sobrenome ="Fujita Felício"

var nomecompleto= nome + " " + sobrenome;

alert(total);
alert(nomecompleto)

/*
 var - vira uma variável utilizavel em todos os momentos do código.
 se a variável for criada num if e precisar ser utilizada depois de
 sair do if utilize var  

 let - é uma variável que não vai existir fora de uma caixa 
 de códigos, evita gasto de memória. se a variável for criada num if
 porém não será mais utilizada fora dele utilize let para poupar 
 a memória

 const - cria uma variável de valor constante.
 não é possível receber outros valores
*/